<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/travelselection_background"
    android:orientation="vertical"
    android:paddingBottom="0dp"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/realtimecallprogress"
            style="@style/RealtimeProgress"
            android:visibility="gone"></TextView>

        <Spinner
            android:id="@+id/fromSpinner"
            style="@style/TravelSelectionSpinner" />

        <Spinner
            android:id="@+id/toSpinner"
            style="@style/TravelSelectionSpinner" />

        <Spinner
            android:id="@+id/timeOptionsSpinner"
            style="@style/TravelSelectionSpinner" />

        <Button
            android:id="@+id/finddepartures"
            style="@style/StandardButton"
            android:text="Kjøyr"
            android:textSize="20dp" />
    </LinearLayout>

    <ListView
        android:id="@+id/travels"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:drawSelectorOnTop="false" />

</LinearLayout>